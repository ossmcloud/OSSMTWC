TODO:
-----

UI Panels:
    implement support for tables (add/edit/delete)
        MAYBE would be wise to have the sub list lazy loading on screen
        user TableControl here
        also, need to load events
        

Site Info Page: 
    // @@TODO: we need to cater for data changed on a linked table

Site Finder
    move to component (as site info panel) and test on site access locator










________________________________________________________________________________________________________________________________________________________________

TODO-DATA:
----------
    SEE: C:\e_drive\OSSM Developments\OSSMTWC\.docs\data\Sites Objects Schemas REV7 ISSUED (NetSuite Actions).xlsx
        SITE Fields => create fields

    wherever we have the OOSMTWC - Structur or OSSMTWC - Accomodation or OSSMTWC - Generator table we need to replace it with the OSSMTWC - Infrastructure table
        select      c.scriptid, c.name, cf.fieldvaluetype as field_type, LOWER(cf.scriptid) as field_id, cf.name as field_label, lower(l.scriptid) as field_foreign_table
        from        customfield cf
        join        customrecordtype c on c.internalid = cf.recordtype
        left join   customrecordtype l on l.internalid = cf.fieldvaluetyperecord
        where       (
                    l.scriptid = UPPER('customrecord_twc_ac')
              or    l.scriptid = UPPER('customrecord_twc_str_')
              or    l.scriptid = UPPER('customrecord_twc_gen')
        )
        order by id


    
________________________________________________________________________________________________________________________________________________________________

ADD FEATURE:
------------

01. Create Files:
    FileCabinet\SuiteScripts\OSSMTWC\ui\oTWC_{FEATURE_NAME}_sl.js
    FileCabinet\SuiteScripts\OSSMTWC\ui\oTWC_{FEATURE_NAME}_cs.js
    FileCabinet\SuiteScripts\OSSMTWC\ui\js\oTWC_{FEATURE_NAME}.js
    FileCabinet\SuiteScripts\OSSMTWC\ui\modules\oTWC_{FEATURE_NAME}Utils.js

02. Deploy suitelet (get script id)
    NOTE: set external role: allow TWC Customer Center

03. Add record to table: OSSMTWC - Role Feature List




________________________________________________________________________________________________________________________________________________________________

TO REVIEW:
----------

Radix Role is assigned to the NS Role
    This means that a user could assign an 'Admin' radix role to a customer or vendor which should not be done under any circumstances
    We need to figure out a way to avoid this (cannot use Sourcind on the custom field as 'Center Type' is not available as List/Record option)







________________________________________________________________________________________________________________________________________________________________

BEFORE GO-LIVE:
---------------
    
DELETE INACTIVE FIELDS / TABLES